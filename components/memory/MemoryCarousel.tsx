"use client";
import { useState } from "react"; import type { Post } from "@/types";
export default function MemoryCarousel({post}:{post:Post}){const [index,setIndex]=useState(0);const slides=[post.track.artwork_url,...post.media.map(m=>m.public_url||m.storage_path)].filter(Boolean) as string[];return <><div className="memoryViewport" onScroll={e=>{const el=e.currentTarget; setIndex(Math.round(el.scrollLeft/el.clientWidth));}}>{slides.map((src,i)=><div className="memorySlide" key={`${src}-${i}`}>{<img src={src} alt={i===0?`Capa de ${post.track.title}`:`Memória ${i} de ${post.author.display_name}`}/>} {i===0&&<div className="artworkOverlay"><strong>{post.track.title}</strong><span>{post.track.artist}</span></div>}</div>)}</div><div className="dots" aria-label={`${index+1} de ${slides.length}`}>{slides.map((_,i)=><span className={i===index?"activeDot":""} key={i}>●</span>)}</div></>}

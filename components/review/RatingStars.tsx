"use client";
export function RatingStars({value}:{value:number}){ const full=Math.floor(value); const half=value-full>=.5; return <div className="rating" aria-label={`${value} de 5 estrelas`}>{Array.from({length:5},(_,i)=>i<full?"★":i===full&&half?"◐":"☆").join("")}</div> }
export function RatingInput({value,onChange}:{value:number;onChange:(v:number)=>void}){return <div className="ratingInput" role="group" aria-label="Nota"><button type="button" className="secondary" onClick={()=>onChange(Math.max(.5,value-.5))}>− ½</button>{[1,2,3,4,5].map(n=><button type="button" aria-label={`${n} estrelas`} className="starBtn" key={n} onClick={()=>onChange(n)}>{value>=n?"★":"☆"}</button>)}<button type="button" className="secondary" onClick={()=>onChange(Math.min(5,value+.5))}>+ ½</button><span className="subtle" style={{alignSelf:"center"}}>{value||"—"}</span></div>}

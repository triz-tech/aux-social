"use client";
import { Link2, Mic2, Search, X } from "lucide-react"; import { AnimatePresence, motion } from "motion/react"; import { useRouter } from "next/navigation";
export default function CreateSheet({open,onClose}:{open:boolean;onClose:()=>void}){const router=useRouter();function go(mode:string){onClose();router.push(`/new?mode=${mode}`)}return <AnimatePresence>{open&&<motion.div className="sheetBackdrop" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onMouseDown={e=>{if(e.currentTarget===e.target)onClose()}}><motion.section className="sheet" initial={{y:"100%"}} animate={{y:0}} exit={{y:"100%"}} transition={{type:"spring",stiffness:360,damping:34}}><div className="handle"/><button className="iconBtn" style={{float:"right"}} aria-label="Fechar" onClick={onClose}><X/></button><div style={{textAlign:"center",marginBottom:20}}><div className="brand">aux.</div><p className="subtle">Qual é a música?</p></div><button className="choice" onClick={()=>go("listen")}><span className="choiceIcon"><Mic2/></span><div><strong>O que tá tocando?</strong><span>ouve alguns segundos e tenta identificar</span></div></button><button className="choice" onClick={()=>go("link")}><span className="choiceIcon"><Link2/></span><div><strong>Colar link</strong><span>Spotify, Apple Music ou Deezer</span></div></button><button className="choice" onClick={()=>go("search")}><span className="choiceIcon"><Search/></span><div><strong>Buscar música</strong><span>nome, artista ou álbum</span></div></button></motion.section></motion.div>}</AnimatePresence>}

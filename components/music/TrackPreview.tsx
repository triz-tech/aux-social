import type { ResolvedTrack } from "@/types";
export default function TrackPreview({track,onClick}:{track:ResolvedTrack;onClick?:()=>void}){const content=<>{track.artwork_url?<img src={track.artwork_url} alt={`Capa de ${track.title}`}/>:<div style={{width:70,height:70,borderRadius:14,background:"#eee"}}/>}<div style={{textAlign:"left"}}><strong>{track.title}</strong><span>{track.artist}</span><span>{track.album}</span></div></>;if(onClick)return <button type="button" className="trackPreview" onClick={onClick}>{content}</button>;return <div className="trackPreview">{content}</div>}

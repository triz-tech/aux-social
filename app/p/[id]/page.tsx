import { notFound } from "next/navigation"; import PostCard from "@/components/post/PostCard"; import { IS_DEMO } from "@/lib/config"; import { demoPosts } from "@/lib/data/demo"; import { getPost } from "@/lib/data/queries";
export default async function PostPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const post=IS_DEMO?demoPosts.find(p=>p.id===id)??demoPosts[0]:await getPost(id);if(!post)notFound();return <main className="shell"><header className="topbar"><a className="brand" href="/">aux.</a><span className="pill">publicação</span></header><div className="feed"><PostCard post={post}/></div></main>}
